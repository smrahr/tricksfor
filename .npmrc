# Continue on error for postinstall scripts
ignore-scripts=false

# Disable audit during install (speeds up CI/CD)
audit=false

# Disable fund messages
fund=false

# Use legacy peer dependencies (avoids conflicts)
legacy-peer-deps=false
